import { View, Text, Button } from 'react-native'
import React from 'react'

const Buttons = () => {
    return (
        <View>
            <Button onPress={() => { }}>Toggle</Button>
        </View>
    )
}

export default Button